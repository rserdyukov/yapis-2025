# Вариант - 12

## Требования к разрабатываемому языку

1. Встроенные типы — не менее трёх  
2. Возможность инициализации переменных всех типов при объявлении: `<тип>? <имя_переменной> = <выражение>`.  
   1. Инициализирующее выражение может быть константным.  
3. Встроенные операции — не менее десяти.  
4. Встроенные функции.  
   1. Встроенные функции ввода/вывода для работы со встроенными типами: `read()`, `write()`.  
5. Использование сложных выражений (составных и со скобками).  
6. Блочный оператор.  
7. Управляющие структуры.  
   1. Условный оператор (`if-then-else`).  
   2. Операторы цикла (`while` и `until`).  
   3. Оператор цикла с итерациями (`for`).  
8. Пользовательские подпрограммы.  
   1. Передача и возврат параметров.  
   2. Задание локальной и глобальной области видимости для имён переменных.

## Вариант свойств языка

1. Объявление переменных  
   - Неявное  
2. Преобразование типов  
   - Неявное
3. Оператор присваивания  
   - Одноцелевое, например, `a = b`  
4. Структуры, ограничивающие область видимости  
   - Подпрограммы и блочные операторы  
5. Маркер блочного оператора  
   - Неявный, как в Python  
6. Условные операторы  
   - Двух вариантный оператор и многовариантный switch-case  
7. Перегрузка подпрограмм  
   - Отсутствует 
8. Передача параметров в подпрограмму  
   - По ссылке  
9. Допустимое место объявления подпрограмм:  
   - В любом месте программы, также и внутри другой подпрограммы.

## Вариант языка
 
2. Язык для работы с векторами и матрицами
   
   - 2.1. Встроенные типы: vector, matrix
   
   - 2.2. Операции: v + v, v - v, n * v, v * v, |v|, m + m, m - m, m * n, m * m, m[n], m[n] * n, |m|, v * m

## Вариант целевого кода
 
4. Байт-код CPython (.pyc)


## Запуск
Вот готовый файл **`README.md`**, который описывает все шаги по запуску проекта, включая автоматический и ручной способы.

Сохраните этот текст в файл с именем **`README.md`** в корне вашего проекта (рядом с `run.sh` и `run_tests.py`).

```markdown
# VecLang Compiler

Компилятор собственного языка программирования для работы с векторами и матрицами. Проект генерирует байт-код Python (`.pyc`) и исполняет его.

## 📋 Требования

Для работы проекта **критически важно** использование **Python 3.12** (или новее).
Проект использует библиотеку `bytecode`, которая несовместима с Python 3.10 или 3.11.

Проверьте версию перед запуском:
```bash
python --version
# или
py --version
```

---

## 🚀 Быстрый запуск (Рекомендуется)

В корне проекта находится скрипт автоматизации `run.sh`. Он выполняет все необходимые действия:
1. Создает виртуальное окружение.
2. Устанавливает зависимости.
3. Генерирует Python-файлы из грамматики ANTLR (`.g4`).
4. Запускает все тесты.

**Запуск в Git Bash (Windows) / Linux / macOS:**
```bash
./run.sh
```

---

## 🛠 Ручная установка и запуск

Если вы не хотите использовать скрипт `run.sh`, выполните следующие шаги последовательно.

### 1. Создание и активация окружения

```bash
# Создание venv
python -m venv .venv

# Активация (Windows Git Bash / MINGW)
source .venv/Scripts/activate

# Активация (Windows CMD)
.venv\Scripts\activate.bat

# Активация (Linux / macOS)
source .venv/bin/activate
```

### 2. Установка зависимостей

```bash
pip install -r compiler/requirements.txt
```

### 3. Генерация парсера ANTLR

Это обязательный шаг. Без него не появится папка `compiler/generated`.

```bash
# Находясь в корне проекта:
antlr4 -Dlanguage=Python3 -visitor -no-listener -o compiler/generated compiler/VecLang.g4
```

### 4. Запуск тестов

**Автоматический прогон всех файлов из папки `example/`:**
```bash
python run_tests.py
```

**Ручной запуск одного файла:**
```bash
# Формат: python <путь_к_компилятору> <путь_к_файлу_vec>
python compiler/main.py example/example1.vec
```


