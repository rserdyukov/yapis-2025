//Проверка, что глобальная переменная объявлена в функции void main()
graph checkMultiAssignment() {
    global node VAR = node("node");
    node a = node("1");
    arc b = arc(a, a);
    return graph([a],[b]);
}

void main() {
   checkMultiAssignment();
}