# ПРИМЕР 1 | ОШИБКИ В ОБЪЯВЛЕНИЯХ ФУНКЦИЙ

func get_zero(int dummy) void { # ОШИБКА: Пропущена стрелка '->'
    return 0;
}

func add(int a; int b) -> int { # ОШИБКА: Параметры должны разделяться запятой ','
    return a + b;
}