# ПРИМЕР 4 | ОШИБКИ В ВЫРАЖЕНИЯХ

func calculate() -> void {
    int result = 10 * + 5; # ОШИБКА: Два бинарных оператора подряд
}

func main() -> void {
    int x = (5 + (10 * 2); # ОШИБКА: Отсутствует закрывающая скобка ')'
}