# ПРИМЕР 2 | ОШИБКИ В ОПЕРАТОРАХ

func main() -> void {
    int x = 10 # ОШИБКА: Пропущен токен ';' в конце оператора
    int y = 20;
}

func main() -> void {
    if (true) then
        int x = 5; # ОШИБКА: После 'then' ожидается блок кода в '{...}'
}