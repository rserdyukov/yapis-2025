# ПРИМЕР 5 | ПРОЧИЕ СЕМАНТИЧЕСКИЕ ОШИБКИ

func main() -> void {
    int x = 10;

    # ОШИБКА: 'x' - это переменная, а не функция. Используйте ее без '()'
    int y = x(); 

    # ОШИБКА: 'print' - это функция, а не переменная, ей нельзя присвоить значение
    print = "new text"; 
    
    str s = "hello";
    s++; # ОШИБКА: Оператор инкремента/декремента применим только к числовым типам
}