string x = "Привет";
out("x до выполнения цикла в функици: " + x);

for (integer i = 0; i <= 10; i++) {
    integer x = 5;

    while (x < 20) {
        out("x в условном цикле" + x++);
    }

    out("x во время выполнения цикла в функции: " + x);
}

out("x после выполнения цикла в функции: " + x);