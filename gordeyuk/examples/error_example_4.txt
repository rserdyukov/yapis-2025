d s = 'ddd'        // неправильный тип 'd'
int a + 5          // вместо '=' используется '+'
int b = 2 2        // два значения без запятой
i = i ++ 1         // двойной оператор '++'
banana apple;      // конструкция, не являющаяся ни функцией, ни переменной
