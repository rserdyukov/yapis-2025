# === Пример 5: Ошибки в операторах ===

# Ошибка: неправильный синтаксис return
return

# Ошибка: отсутствует выражение после return
func test() -> int:
    return

# Ошибка: неправильный синтаксис print
print("test"

# Ошибка: отсутствует закрывающая скобка в print
print("test", "test2"

# Ошибка: неправильный синтаксис for
for i in range(10
    print(i)

