# === Пример с ошибками ===

# Ошибка 1: отсутствует двоеточие после if
x = 10
if x > 5
    print("Больше 5")

# Ошибка 2: неправильные отступы
func test():
    y = 20
  z = 30  # неправильный отступ

# Ошибка 3: незакрытая скобка
orders = Table(
    id = Сolumn(int),
    name = Сolumn(str

# Ошибка 4: неправильный синтаксис switch
switch(x):
    case 1
        return "один"

# Ошибка 5: отсутствует оператор
func incomplete():
    a = 5

