# === Пример: Работа с таблицами ===
orders = Table(
    id = Сolumn(int),
    name = Сolumn(str)
)

# Правильное использование
orders += row(1, "Alice")

# Ошибка: использование несуществующей таблицы
unknown_table += row(1, "Test")

# Ошибка: операция с неправильным типом
x = 10
x -= 5  # Ошибка: операция '-=' применима только к таблицам

# Ошибка: обращение к члену не-таблицы
y = "string"
y.id = 1  # Ошибка: y не является таблицей

