# === Пример 4: Неверное использование специальных типов (table, row, column) ===
# Операции, допустимые только для определённых типов

# Правильное использование
orders = Table(
    id = Сolumn(int),
    name = Сolumn(str)
)

# Ошибка: операция += применима только к таблицам
x = 10
x += 5

# Ошибка: операция -= применима только к таблицам
y = "test"
# y -= "t"  # Синтаксически некорректно, пропускаем

# Ошибка: обращение к члену не-таблицы
z = 5
z.id = 1  # Ошибка: z не является таблицей

# Ошибка: обращение к члену не-таблицы
str_var = "hello"
str_var.value = 10

