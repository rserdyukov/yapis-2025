# === Пример 4: Ошибки в объявлениях ===

# Ошибка: неправильный синтаксис типа
x: int[ = 10

# Ошибка: отсутствует имя переменной
: int = 10

# Ошибка: неправильный синтаксис таблицы
orders = Table(
    id = Сolumn(int
    name = Сolumn(str)
)

# Ошибка: отсутствует запятая
orders = Table(
    id = Сolumn(int)
    name = Сolumn(str)
)

