# === Пример 1: Отсутствующие скобки и двоеточия ===

# Ошибка: отсутствует двоеточие после if
if x > 5
    print("Больше 5")

# Ошибка: отсутствует двоеточие после for
for i in range(10)
    print(i)

# Ошибка: незакрытая скобка
func test(x: int
    return x

# Ошибка: отсутствует открывающая скобка
func test2 x: int):
    return x

